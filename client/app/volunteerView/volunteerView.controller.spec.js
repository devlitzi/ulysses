'use strict';

describe('Controller: VolunteerViewCtrl', function () {

  // load the controller's module
  beforeEach(module('ulyssesApp'));

  var VolunteerViewCtrl, scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   VolunteerViewCtrl = $controller('VolunteerViewCtrl', {
  //     $scope: scope
  //   });
  // }));
  //
  // it('should ...', function () {
  //   expect(1).toEqual(1);
  // });
});
